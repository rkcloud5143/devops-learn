# Kubernetes — Fundamentals

## Why Kubernetes?
```
Docker Compose:                    Kubernetes:
┌──────────────────┐              ┌──────────────────────────┐
│ Good for 1 server│              │ Good for production      │
│                  │              │                          │
│ - Single host    │              │ - Multiple hosts         │
│ - Manual scaling │              │ - Auto scaling           │
│ - No self-heal   │              │ - Self-healing           │
│ - Dev/testing    │              │ - Rolling updates        │
│                  │              │ - Load balancing         │
└──────────────────┘              └──────────────────────────┘
```

## Architecture
```
┌─── Kubernetes Cluster ──────────────────────────────────┐
│                                                          │
│  ┌─── Control Plane (Master) ────────────────────────┐  │
│  │  API Server ← you talk to this (kubectl)          │  │
│  │  Scheduler  ← decides which node runs a pod       │  │
│  │  Controller ← ensures desired state = actual state│  │
│  │  etcd       ← stores all cluster data             │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  ┌─── Worker Node 1 ──────┐  ┌─── Worker Node 2 ─────┐ │
│  │  ┌─────┐  ┌─────┐      │  │  ┌─────┐  ┌─────┐     │ │
│  │  │ Pod │  │ Pod │      │  │  │ Pod │  │ Pod │     │ │
│  │  │nginx│  │ app │      │  │  │ app │  │redis│     │ │
│  │  └─────┘  └─────┘      │  │  └─────┘  └─────┘     │ │
│  │                         │  │                        │ │
│  │  kubelet (agent)        │  │  kubelet (agent)       │ │
│  │  kube-proxy (networking)│  │  kube-proxy            │ │
│  └─────────────────────────┘  └────────────────────────┘ │
└──────────────────────────────────────────────────────────┘

Pod = smallest unit, usually 1 container
Node = a server (EC2 instance in AWS)
Cluster = group of nodes
```

## Checklist
- [ ] Install minikube, run a local Kubernetes cluster
